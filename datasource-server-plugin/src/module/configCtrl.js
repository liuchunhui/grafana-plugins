export default class GenericConfigCtrl {
    constructor() {
        // console.log('GenericConfigCtrl:', this, this.current, this.meta);
        // this.current['description'] = this.current['description'] || '';  // 数据源配置添加api字段
    }
}

GenericConfigCtrl.templateUrl = './page/config.html';